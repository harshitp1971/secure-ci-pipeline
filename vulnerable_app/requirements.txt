# Runtime dependencies for the demo app — SECURE baseline (main branch).
#
# Pinned to current, patched releases. These resolve clean under pip-audit on
# the CI Python (3.12); the fixes for the most recent advisories require
# Python >= 3.10, which the pipeline uses.
#
# The add-user-feature branch adds an outdated, CVE-affected dependency on top
# of this set to demonstrate the dependency scanner blocking a pull request.
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
click==8.3.3
gunicorn==23.0.0
