# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local SQLite database created at runtime
*.db

# Generated scan output (regenerated by the pipeline; committed samples live
# under samples/ instead)
artifacts/

# Raw ZAP reports dropped in the repository root by the ZAP action.
# Anchored with a leading slash so the committed samples/zap.json is NOT ignored.
/zap.json
/report_json.json
/report_html.html
/report_md.md
.zap/

# OS / editor noise
.DS_Store
*.log
